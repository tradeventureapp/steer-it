node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite
.vercel

# Local tooling state
.claude/settings.local.json

# Pre-existing local file — looks like a credential, kept out of the repo
databaze.txt

# Fury sprite: public/Fury.png is now the CLEAN Tradeventure-branded image (own IP, zero
# third-party logos) — it SHIPS. The original spaced-name export stays LOCAL only (the
# loader reads the no-space public/Fury.png copy).
public/Fury tradeventure.png
